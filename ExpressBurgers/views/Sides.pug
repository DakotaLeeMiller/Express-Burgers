extends layout

block content
    h1 Sides
  
    ul
        each val, x in config.sides
            li(id="menu")
                div(id="center")
                    div(class="items")
                        div(class="col")
                            img(src=val.image)
                        div(class="col")
                            div(class="name") #{val.name}
                            div(class="price") $#{val.price}
                            div(class="desc") #{val.desc}