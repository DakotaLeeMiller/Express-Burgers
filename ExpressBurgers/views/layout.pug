doctype html
html
    head
        link(href="style.css" rel="stylesheet")
        meta(charset='utf-8')
        title Space Junk Diner

    body
        header
            h1
                a(href=config.navi[0][1])
                    img(id="logo" src="/img/spaceDinerIcon.png")
                em(id="title") SPACE JUNK DINER
            
            nav
                ul
                    each item in config.menu
                        a(href=item[1] class="menu_text")
                            div(class="menu_item")
                                li(id=item[1])  #{item[0]}

            a(href=config.navi[1][1])
                div(id="right") Direction
            
        div(id="clear")

        block content

        footer 
            em Website by Benito Luna, Dakota Miller, Cody Watts © 2018